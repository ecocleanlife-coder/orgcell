[중요: 상충 해결 원칙] > 이 문서의 내용은 다른 모든 문서(CLAUDE.md, rules/*.md 등)보다 우선한다. 만약 다른 문서와 이 문서의 내용이 충돌할 경우, 무조건 이 문서의 규칙을 따르고, 충돌 사실을 사용자에게 보고하라.

# Orgcell 가족유산박물관 — 코딩 표준 규칙서

**작성일:** 2026-04-05
**버전:** v3.0 (최종 확정)
**원칙:** 본 문서의 설계 원칙은 모든 코드 구현의 최우선 순위임.

---

## 1. 서비스 정체성

- 한국어: **가족유산박물관**
- 영어: **Family Heritage Museum**
- 태그라인: "FamilySearch가 죽은 자의 도서관이라면, Orgcell은 산 자의 박물관입니다."
- "디지털" 단어 사용 금지

---

## 2. 상향 배치 및 수평 균형 원칙 (The Golden Upward Rules)

중앙 집합 원칙 (Centering): 모든 부모 노드의 기본 X 좌표는 그들이 배출한 '자녀들(Children) 전체 집단의 산술적 중앙값' 수직 위로 설정한다. 뿌리는 자손들의 중심을 잡아야 한다.

---

## 3. 부모의 배치

관장의 형제가 없어 가계도의 한쪽 날개가 비어 보일 수 있는 특수 상황(예: 남편 측 부모 표시 시)에는, 시각적 빈약함을 방지하기 위해 부(父)와 모(母)를 배치하되 "남편 부모 그룹의 중심 좌표는 X = -300, 아내 부모 그룹의 중심 좌표는 X = +300으로 강제한다."

이는 형제가 없더라도 부부라는 독립적인 존재감을 시각적으로 확보하기 위한 조치이다.

---

## 4. card 표시

- 사진, 이름, ID, 대표정보 (최대 3줄)
- 호버 시 "OOO 박물관" 툴팁만
- 호버 메뉴 없음
- 고인도 동일 표시, 생존기간만 (예: 1930~2005)

---

## 5. 블록 입체감

**모든 블록 — 약한 입체감:**
```css
.folder-card {
  border: 1px solid #C4A882;
  background: #FAFAF5;
  border-right: 2px solid #b09060;
  border-bottom: 2px solid #9a7a50;
  box-shadow: 2px 2px 0 #c4a87a;
}
```

**관장 부부 — 강한 입체감:**
```css
.folder-card.curator {
  border: 2px solid #8B7355;
  background: #FDF8F0;
  border-right: 4px solid #9a7a50;
  border-bottom: 4px solid #7a6040;
  box-shadow: 3px 3px 0 #c4a87a, 6px 6px 0 #b09060;
}
```

---

## 6. 클릭 동작

"타인 카드 클릭 시 즉시 이동하지 않고, **'XXX 님의 박물관으로 이동하시겠습니까?'**라는 확인 모달을 띄운다."

"확인 클릭 시: 현재 화면의 모든 요소를 페이드 아웃(Clear)하고, 화면 중앙에 **'XXX 님의 박물관으로 이동합니다...'**라는 안내 문구를 표시한다."

"이동 후 처리: 해당 인물의 등록 데이터가 있으면 그 내용으로 트리를 구성하되, 데이터가 전혀 없는 유령 회원일 경우 '임시 부'와 '임시 모' 노드만을 생성하되 아래의 원칙에 따라 기본 박물관 형태를 구성한다."

**가상 노드(Stub) 생성 및 소멸 원칙**

생성 조건: 가상 부모(임시 부/모)는 해당 인물의 기록에 부(父)와 모(母) 데이터가 동시에 존재하지 않을 때만 생성한다.

실명 우선(Bypass): 단 한 명의 실명이라도 등록되거나 저장되는 즉시, 가상 노드 로직은 파기되고 실제 데이터 기반 배치를 수행한다.

- 🏠 버튼 → 원래 박물관으로 복귀

**더블클릭 동작:**
카드 더블클릭 시 → `/{subdomain}/archive` 자료실로 이동.
해당 인물의 관계 탭 자동 선택 + 정보 폼 자동 로드.
(관장 본인/배우자 더블클릭 포함)

---

## 7. ID 체계

- 형식: 국가코드-5자리영문숫자랜덤 (예: KR-A3K7B)
- 국가코드 기준: 한국어→KR, 영어→접속지 IP (US, GB 등)
- 혼동 방지: O/0, I/1, L 제외
- 사용 문자: 2-9, A-H, J-N, P-Z (30자)
- 부모/자녀 생성 시 부모 국가코드 상속
- 카드에 이름 아래 ID 표시

---

## 8. 자료실 레이아웃

┌─────────────────────────────────┬───────────────┐
│ [사진 불러오기]
마우스/손가락으로 크기·위치 조정    │ [사진자료실]  │
│ 이름: [____________]            │ [주요자료실]  │
│ 생년월일: [__] ○양력 ○음력      │ [주요약력]    │
│ 성별: ○남 ○여                   │ [자서전]      │
│ 대표정보1: [___________]        │ [작품실]      │
│ 대표정보2: [___________]        │ [육성녹음]    │
│ 대표정보3: [___________]        │ [공유앨범]    │
│                                 │ ───────────  │
│ [취소]  [제거]  [완성]          │ [초대하기]    │
│                                 │ [입장권발급]  │
│                                 │ [접근요청관리]│
└─────────────────────────────────┴───────────────┘

[사진 업로드]     이름: [___________]
마우스/손가락으로  생년월일: [___________]
크기·위치 조정     성별: ○남 ○여
                  ☐ 사망 → 사망일: [___]
                  대표정보1: [___________]
                  대표정보2: [___________]
                  대표정보3: [___________]

[생성]  [수정]  [제거]

- [완성]: 트리 즉시 반영
- [수정]: 기존 인물 정보 수정 → 트리 업데이트
- [제거]: "정말 관계를 해제하시겠습니까?" 확인 후 삭제

### 우측 40% — 메뉴 버튼 (세로 배치)
[사진자료실]  → 공개 시 "사진전시관"
[주요자료실]  → 공개 시 "자료전시관"
[주요약력]    → 공개 시 "약력전시관"
[자서전]      → 공개 시 "자서전전시관"
[작품실]      → 공개 시 "작품전시관"
[육성녹음]    → 공개 시 "음성전시관"
[공유앨범]    → 생성 즉시 상단 메뉴에 노출
─────────────
[초대하기]
[입장권발급]
[접근요청관리]


---

## 9. 관계 탭 동작 규칙

- 탭 선택 시 해당 인물 기존 데이터 자동 로드
- 등록된 인물 → [수정][제거] 활성화, [생성] 비활성화
- 미등록 인물 → [생성] 활성화, [수정][제거] 비활성화
- [제거] 클릭 시: "정말 관계를 해제하시겠습니까?" 확인 후
  person_relations 삭제
- 자료실 내 가족 관계 시각화 시, 부모 노드는 해당 당사자
  카드 바로 위에 수직 정렬 (부부 박스 중앙 아님)---

## 10. 공개 → 전시관 자동 생성

- 자료실에서 공개 체크된 자료는 박물관 상단 메뉴에 전시관으로 자동 추가
- 공개 자료가 없는 전시관은 메뉴에 안 나타남

---

## 11. 공유앨범

### 생성 (자료실 → [공유앨범] 클릭 → 하단):
```
새 공유앨범 만들기

제목: [2025 하와이 가족여행_______]
공개범위: ○ 전체공개 ○ 가족만 ○ 링크만

[사진 불러오기]  [초대하기]  [생성]

─── 내 공유앨범 목록 ───

■ 2025 하와이 가족여행    📸 234장
  [공유링크 복사] [초대하기] [삭제]

■ 2025 NCI Cancun        📸 567장
  [공유링크 복사] [초대하기] [삭제]
```

### 공유앨범 전시관 (상단 메뉴에서 클릭 시):
- 로그인 없이 사진 업로드 + 다운로드 가능
- 각 사진마다 체크박스 → 선택 다운로드
- 중복 사진 자동 감지 + 제거
- 사진 올리기 시 "보유하고 계신 사진을 정리해 드릴까요?" 안내
- 생성 즉시 박물관 상단 메뉴에 노출
- 하단에 Orgcell 가입 유도

---

## 12. 상단 메뉴 스타일

- 자판기 버튼 스타일 (둥근 모서리, 입체감, 누르면 2px 내려감)
- 기존 메뉴: 금색 톤 (#FDF8F0 배경, #C4A882 테두리)
- 공유앨범: 푸른 톤 (#F0F5FD 배경, #8BA5C4 테두리), 📷 아이콘 통일
- 메뉴가 많아지면 자연스럽게 다음 줄로 넘어감

---

## 13. 조상전시관

- 각 사람이 본인 박물관에 추가 가능
- 자료는 조상의 자료실에 먼저 기록 (원본)
- 원하는 것을 선택하여 본인 조상전시관에 전시
- 상속인이 비공개 시 다른 사람 전시관에서도 자동으로 내려감

---

## 14. 권한 체계

- 관장 → 모든 인물 자료실 접근 가능
- 관장이 등록한 인물 → 당사자 계정 만들기 전까지 관장 전체 권한
- 당사자 계정 생성 후 → 당사자가 주인
- 외부인 클릭 → AccessDeniedModal → 관장에게 요청

---

## 15. 디지털 유산 승계

- 망자의 박물관 관장 권한은 유언 또는 법적 상속인이 승계
- 공개 권한도 상속인이 가짐
- 상속인이 자료 추가/수정/삭제 가능

---

## 16. 프라이버시 3레이어

- PUBLIC: 이름, 관계, 공개 사진
- FAMILY: 생년월일, 추가 사진 (8촌 기본)
- PRIVATE: 비공개 메모, 개인정보

---

## 17. 초대 시스템

- 이메일 / SMS / 링크 복사
- 수락/거절 → 노출 선택 (이름 전체 / 성만 / 익명)
- 거절자 → RefusedPersonBox

---

## 18. 기부 시스템

### 박물관 초기 화면:
- 한쪽에 [💛 $1 기부하기] 버튼만 표시

### 클릭 시:
```
💛 기부하기

☑ $1
☐ $[________]

이름 (선택): [________]

[결제하기]
```
- $1 기본 체크, 아래 금액란에 타이핑 시 체크 자동 이동
- 이름은 선택, 안 쓰면 익명 표시
- Stripe 연동
- 기부자 정보는 Stripe 결제 기록으로 관리자에게만 보임
- 관장 자료실에서 기부 내역 확인 가능

---

## 19. 데이터 원칙

- person_relations 테이블 기반 재귀 계산
- family-chart 라이브러리 사용 금지
- BYOS (Bring Your Own Storage) 원칙
- relation_type: parent-child, spouse, sibling, birth-parent

### 개인 폴더 원칙
- 인물 생성 시 서버에 `uploads/persons/{personId}/` 폴더 자동 생성
- 프로필 사진 업로드 시 해당 폴더에 `profile.jpg` 로 저장
- 폴더 기준: person.id (정수 PK)
- EC2 경로: `/opt/orgcell/backend/uploads/persons/{personId}/profile.jpg`

---

## 20. 저작권

- 모든 콘텐츠 무단 복제/배포 금지
- footer 경고 상시 표시
- 이용약관 포함

---

## 21. 검색 기능

박물관 초기 화면 상단에 검색바:
```
[🔍 이름 또는 ID로 검색 _______________]
```

- 이름 검색: 한글/영문 모두 지원
- ID 검색: KR-A3K7B 형식으로 직접 검색
- 검색 결과: 해당 인물의 박물관으로 이동
- PUBLIC 정보만 검색 가능 (PRIVATE 인물은 검색에 안 나옴)
- 거절자(RefusedPersonBox)는 검색 결과에서 제외

---

## 22. 가족나무 표시 범위 (v3.7)

### 화면에 표시하는 것 (z=0)
*배우자는 실존하는 경우에만 한정하여 표시한다.*

1. **관장 본인** + 배우자
2. **관장의 자녀**와 그 배우자 + 그들의 자녀/자손 증손주까지 및 배우자들 전체 자손들 (3대 이후는 **Opacity 0.3** 안개 처리)
3. **관장의 형제**와 그 배우자
4. **관장의 배우자의 형제**와 그 배우자
5. **관장의 부모**
6. **관장의 배우자 부모**

### 화면 표시 제외 (z=1, 수장고 보관)
- 위 리스트에 해당하지 않는 모든 인물.
- 데이터는 유지하되 타인의 '표시 범위'에 해당할 때만 그들의 트리에서 활성화됨.

**[철칙: 엄격한 격리]** z=0(표시 대상) 리스트에 명시되지 않은 인물은 어떠한 경우에도 화면(Canvas)에 렌더링되어서는 안 된다.
트리 생성 마지막 단계에서 z=1인 인물이 z=0 구역에 좌표를 가지고 있는지 전수 조사하여 즉시 제거한다.

**상향식 직계 배치 (Upwards Layout)**
- 부모 세대: 관장과 그 배우자의 부모를 각각 그들의 자녀들 중간 위에 배치한다.
- 부계 방계: 관장의 부(父)의 형제와 배우자는 부(父)의 바로 좌측 옆(수평)에 배치한다.
- 모계 방계: 관장의 모(母)의 형제와 배우자는 모(母)의 바로 우측 옆(수평)에 배치한다.

---

## 23. 배치 및 선연결 룰

### 부모→자녀 연결선
- 부부 박스 아래 중간에서 수직선 내림
- 수직선이 자녀들의 수평 연결선 중간에 닿음

### 여러 자녀 (직계에만 적용한다)
- 각 자녀 위 중간에서 형제간 수평선 연결
- 수평선 중간에서 위로 수직선 → 부모 박스 연결
- 자녀 순서: 생년월일 오름차순 (장남/장녀부터) 좌→우

### 자녀에 배우자 생기면
- 자녀 옆에 남좌여우로 배치, 부부 박스로 묶임
- 그만큼 좌우 공간 확장

### 배우자 부모 연결선 (CRITICAL)
- 부부 박스 중앙이 아닌 **해당 배우자 카드 개인 중앙**에서 선 올라감
- 예: 임윤님 부모 선은 "이순호─임윤님" 박스 중앙이 아닌 임윤님 카드 중앙에서 올라감

### 형제 배치 - 자녀가 있어도 이 룰을 유지한다.
- 남편 형제 → 남편 왼쪽으로 260px씩
- 아내 형제 → 아내 오른쪽으로 260px씩
- 형제에 배우자 생기면 옆에 배치 + 카드 + 공간 확장

### 화면 중앙 배치 (배우자가 있을 경우는 부부)
- 초기 화면: 관장(부부) 부부 박스 중심이 화면 가로 정중앙
- 타 박물관 방문 시: 그 박물관장(부부) 박스 중심이 화면 가로 정중앙
- 🏠 복귀 시: 관장 (부부) 박스 중심이 화면 가로 정중앙

---

## 24. UI & Layout 엄격 규정: 가족 트리 렌더링

본 규정은 `VISION.md`의 기획을 화면에 구현하기 위한 '절대적인 픽셀 수치 및 렌더링 기준'이다. 프론트엔드 레이아웃 계산 시 아래의 수치와 조건을 하드코딩 수준으로 최우선 준수해야 한다.

### 24-1. 부부 노드 (Couple Zone) 규격
* **전체 너비 (Width):** 부부를 감싸는 최상단 박스(CoupleBlock)의 너비는 정확히 `440px`로 강제한다.
* **카드 간격 (Gap):** 부부 박스 내부의 두 카드 래퍼(220px + 220px) 사이의 마진/간격은 무조건 `0px`로 완전히 밀착시킨다.
* **사진 간격 보장:** 카드가 0px로 밀착되더라도, 내부의 사진(180x180)끼리 맞붙지 않도록 각 카드의 내부 패딩을 통해 '시각적 간격 40px'을 반드시 유지한다.
* **시각적 묶음:** 카드 개별 테두리(Border)를 금지하고, 440px 부부 박스 전체 배경에 연한 미색(`#F9F7F2`)을 깐다.

### 24-2. 부모 이상 세대 X축 고정 좌표 배치
* **관장 부부 (Main Couple):** 기준점 `X = 0` (화면 정중앙)
* **관장의 부모 (Paternal Parents):** 관장의 형제들의 중심으로 배치하되, 형제가 없을 경우 부모박스 중심 좌표는 `X = -300`으로 강제한다.
* **관장 아내의 부모 (Maternal Parents):** 부인의 형제들의 중심으로 배치하되, 형제가 없을 경우 부모박스 중심 좌표는 `X = +300`으로 강제한다.

### 24-3. 지정된 배치를 제외한 자들의 렌더링 제외 (가지치기)
* 트리를 렌더링 배열(Visible Nodes)로 빌드할 때, 모든 배치가 제외된 자들의 기록은 스토어(데이터)에만 보관하고 화면에는 절대 렌더링하지 않는다.

### 24-4. 애니메이션 및 화면 제어 수치
* **순차 등장 간격:** 위 §22 가족나무 표시 범위의 순서대로 화면에 표시하되, Framer Motion 등의 `delay` 수치는 `0.3초(0.3s)` 간격으로 설정하여 중심에서 바깥쪽으로 전개되도록 한다.
* **리마운트 강제:** 중심 인물(Main Person)이 변경되거나 웜홀 라우팅이 발생할 때, 이전 노드의 좌표 캐시와 뷰포트 상태를 완전히 삭제하고 `navKey` 등을 통해 캔버스의 리마운트를 강제하여 Ghost Node(겹침 현상)를 원천 차단한다.

---

## 25. 코드 동기화 및 저장 원칙 (Sync & Save Rules)

**실시간 반영:** AI는 모든 코드 수정 사항을 설명으로만 끝내지 말고, 실제 로컬 소스 코드 파일(.js, .jsx, .css 등)에 즉시 반영(Write/Save)해야 한다.

**드라이브 동기화:** 수정된 파일은 구글 드라이브(Google Drive) 동기화 폴더를 통해 실시간으로 클라우드에 업데이트되어야 한다.

**완결성 보고:** 작업을 마칠 때마다 수정된 파일 목록과 동기화 여부를 사용자에게 보고하여 데이터 유실을 방지한다.

**최신본 유지:** 모든 개발 작업 시작 전, 컴퓨터에 저장된 최신 MD 파일과 코드를 먼저 읽어 현재 상태를 파악한다.

현재 컴퓨터에 파일이 없을 땐 구글 드라이브에 저장된 최신 MD 파일과 코드를 먼저 읽어 현재 상태를 파악한다.

---

## 26. OPS (Orgcell Path System) — 인물 경로 및 통합 규칙

### 26-1. Path 규칙 (관계 코드)

| 관계 | 코드 | 복수 예시 |
|------|------|-----------|
| 배우자 (아내) | w | — |
| 배우자 (남편) | h | — |
| 아들 | s | s1, s2 |
| 딸 | d | d1, d2 |
| 아버지 | f | — |
| 어머니 | m | — |
| 형제 | b | b1, b2 |
| 자매 | si | si1, si2 |
| 배우자 아버지 | fc | — |
| 배우자 어머니 | mc | — |
| 배우자 형제 | wb | wb1, wb2 |
| 배우자 자매 | ws | ws1, ws2 |

재귀 확장: orgcell/lee/s1/s1 (첫째 아들의 첫째 아들)

### 26-2. Path는 alias, 실체는 person_id

- 모든 인물의 실제 데이터는 person_id(KR-A3K7B)에 귀속
- Path는 영구 유지되는 별칭(alias)
- 동일 person_id를 여러 path가 가리킬 수 있음
- 가입 전: path로만 접근 (ghost 상태)
- 가입 후: path 유지 + person_id로 실체 연결

### 26-3. 자동 통합 정책 (선점 원칙)

철칙: 먼저 생성된 person_id가 canonical. 나중 path는 alias로 흡수.

**매칭 우선순위:**

1. [최강] 부모 2명 모두 일치 → 이름이 달라도 자동 통합
   (부모는 바뀌지 않는다. 이름은 결혼/개명/로마자 표기로 바뀔 수 있다)

2. [강력] 형제 2명 이상 일치 → 자동 통합

3. [일반] 이름 일치 + 생년월일 일치 + 보조 1개 이상 → 자동 통합
   보조 항목: 성별, 전화번호, 이메일, 부모 1명 일치

4. [보류] 이름 + 생년월일만 일치 → 동명이인 가능성, 보류

**통합 처리:**
- 통합 즉시 실행 (승인 대기 없음)
- 양측 관장에게 알림 발송
- 이의 시 분리 요청 버튼 제공 (기본은 통합 유지)

### 26-4. 보안 원칙

**Ghost 격리:**
- Ghost는 관장 박물관 내부에만 존재
- 외부 검색, 노출 완전 금지
- Ghost 상태에서 관장만 데이터 수정 가능

**통합 원칙:**
- Ghost만 자동 통합 허용 (선점 원칙 적용)
- 계정 보유자는 자동 통합 대상 제외
  → pending 상태로 전환 → 본인 승인 후 Linked
- 통합 즉시 양측 관장에게 알림 발송

**Linked 후 권한:**
- Linked 확정 시 해당 인물 데이터 권한은 본인에게 이전
- 관장은 해당 인물 데이터 수정 불가
- 본인이 언제든 연결 해제 가능

**공인 계정 옵션:**
- 공인 계정 신청 시 ghost 등록 및 초대 차단
- 누군가 이메일 입력해도 초대카드 발송 불가

---

### 26-5. 출입증(Access Pass) 시스템

**기본 원칙:**
- 입장권 없는 사람은 박물관 내부를 볼 수 없다
- orgcell.com/lee 접속 시 문패(박물관 이름)만 보임
- [입장권 요청하기] 버튼으로 관장에게 알림 전송
- 관장이 요청 확인 후 초대카드 발송

**초대카드 형식 (가족/손님 동일):**
┌─────────────────────────────┐
│  🏛 OOO 가족유산박물관      │
│                             │
│  OOO님을 초대합니다         │
│                             │
│  [QR코드]                   │
│                             │
│  유효기간: YYYY.MM.DD까지   │
│  (가족은 "평생" 표시)       │
│                             │
│  [박물관 입장하기]          │
└─────────────────────────────┘
- QR코드 스캔 또는 [박물관 입장하기] 클릭 시
  해당 박물관으로 진입
- 카카오/문자/이메일/SNS로 전송

**Legacy Pass (가족 입장권):**
- Linked 확정 시 자동 발급
- 유효기간: 평생
- 본인 계정에 귀속, 복제 불가
- 발급: 자료실 [초대하기] → 초대카드 생성 → 전송

**Visitor Pass (손님 입장권):**
- 유효기간: 3일/1주/1개월/무제한 (관장 설정)
- 동시 접속 인원 제한 (관장 설정)
- 관장이 언제든 무효화 가능
- 발급: 자료실 [입장권 발급] →
  이름(선택) + 유효기간 설정 →
  초대카드(QR포함) 생성 → 전송

**접근 요청 처리:**
- 미입장자 [입장권 요청하기] 클릭
  → 이름 + 연락처 입력
  → 관장에게 알림
- 관장이 자료실 [접근요청관리]에서 확인
  → [발급]: 초대카드 생성 후 전송
  → [거절]: 요청 삭제

**입장권 유효성 검사:**
- 유효 → 박물관 입장
- 만료 → "유효기간이 지난 입장권입니다"
- 무효화 → "사용할 수 없는 입장권입니다"


