\# VISION.md: Orgcell Project Philosophy \& Core Logic



\## 1. Project Vision (프로젝트 비전)

Orgcell은 단순한 족보나 조직도를 넘어, 개인과 가족의 역사를 기록하고 탐험하는 \*\*'글로벌 가족/지인 박물관 네트워크'\*\*이다. 

우리는 데이터를 중앙 서버에 가두지 않고, 각 개인의 저장소(BYOS)에 보관된 독립적인 박물관들을 '웜홀(Wormhole)'을 통해 매끄럽게 연결하여 무한한 관계의 우주를 탐험하는 경험을 제공한다.



\## 2. Tree Layout Algorithm (가족 트리 배치 알고리즘)

가족 트리의 배치는 시각적 안정감과 직관적인 관계 파악을 최우선으로 고려하며, 다음의 논리적 규칙을 따른다.



\* \*\*초점의 원칙 (세대 제한):\*\* \* 트리는 현재 화면의 주인공인 '관장 부부(Main Person)'를 중심으로 렌더링된다.

&#x20; \* 인지적 과부하를 막기 위해 한 화면에 표시되는 윗세대는 \*\*'조부모 세대(Depth 2)'\*\*까지만 렌더링하는 것을 원칙으로 한다.

\* \*\*좌우 분리의 원칙:\*\* \* 관장 부부의 윗세대(뿌리)를 그릴 때, 혈통의 시각적 분리를 명확히 한다.

&#x20; \* 남편(Husband)의 형제와 그들의 후손3대까지, 직계 부모및 조부모까지만 렌더링된다.

&#x20; \* 아내(Wife)의 형제와 그들의 후손은 3대까지, 조상은 직계 부모 까지만 렌더링된다.



\* \*\*수직 확장의 원칙:\*\*

&#x20; \* 자녀 세대는 관장 부부의 아래쪽(Bottom)으로 배치되며, 자녀가 여럿일 경우 중심을 기준으로 좌우 균형을 맞춰 수평 전개된다.



\## 3. Presentation \& UX (애니메이션 및 렌더링 UX)

새로운 박물관(도메인)에 입장하거나 중심 인물이 변경될 때, 사용자에게 '이야기가 펼쳐지는 듯한' 프리미엄 경험을 제공한다.



\* \*\*순차적 등장 (Staggered Reveal):\*\* \* 모든 노드가 한 번에 나타나지 않는다.

&#x20; \* 관장 부부가 가장 먼저 중심에 등장하고, 이후 약 \*\*0.3초의 일정한 간격(Delay)\*\*을 두고 중심에서 바깥쪽(자녀 → 부모 → 조부모)으로 한 명씩 순차적으로 페이드인(Fade-in) 되며 나타난다.

\* \*\*카메라 자동 맞춤 (Auto-Fit \& 줌아웃):\*\* \* 순차 애니메이션이 진행되어 노드가 화면에 추가될 때마다, 렌더링된 전체 가족이 화면 밖으로 잘리지 않도록 뷰포트(카메라)가 자동으로 줌아웃(Scale-down) 되며 모든 인원을 한 화면(Bounding Box) 안에 부드럽게 담아낸다.



\## 4. Fallback \& Data Handling (예외 처리 및 데이터 철학)

데이터가 비어있거나 외부 스토리지 연결이 지연될 때도, 서비스는 절대 멈추지 않고(Crash-free) 사용자에게 친절하게 상황을 안내해야 한다.



\* \*\*우아한 실패 (Graceful Degradation):\*\* \* 웜홀 라우팅 시 외부 드라이브(사진 등) 응답이 지연되더라도, 트리의 뼈대(이름, 관계 선)는 즉시 그려져야 하며, 사진은 백그라운드에서 조용히 재시도된다.

\* \*\*가상 노드 (Virtual Node) 생성:\*\* \* 트리 알고리즘 상 위치해야 할 부모나 조부모의 데이터가 아직 입력되지 않아 비어있는 경우, 트리 구조가 끊어지게 두지 않는다.

&#x20; \* 해당 빈자리에 즉시 '가상 노드'를 생성하여 시각적인 틀을 유지하고, 이름 대신 \*\*"아직 개관전입니다"\*\*라는 메시지를 표시하여 사용자의 호기심을 유발하고 추후 데이터 입력을 유도한다.



\## 5. Core Navigation: Wormhole Architecture

\* 다른 사용자의 박물관으로 이동하는 '웜홀 라우팅' 발생 시, 혹은 내부에서 중심 인물을 변경할 시, 이전 트리 상태(캐시, 뷰포트, 좌표 등)는 \*\*완벽하게 초기화(Hard Reset)\*\*되어야 한다. 

\* 이전 화면의 잔재(Ghost Node)가 새 트리에 겹치는 것을 절대 허용하지 않는다.



\---



> ⚠️ \*\*\[개발자 및 AI 에이전트 주의사항: 문서 역할 분담]\*\*

> 본 `VISION.md`는 프로젝트의 \*\*기획, 렌더링 논리, UX 철학\*\*만을 정의합니다.

> 컴포넌트의 가로/세로 크기(px), 요소 간의 마진/패딩 여백, 색상 코드, 그리고 부모 노드의 구체적인 X/Y 배치 좌표(예: -300, +300 등)와 같은 \*\*모든 수치화된 코딩 지침은 무조건 `ORGCELL\_CODING\_RULES.md`를 최우선으로 준수\*\*해야 합니다. 절대 이 문서에 픽셀이나 좌표 계산식을 하드코딩하지 마십시오.







