VISION.md: Orgcell Project Philosophy & Core Logic
1. Project Vision (프로젝트 비전)
Orgcell은 단순한 족보나 조직도를 넘어, 개인과 가족의 역사를 기록하고 탐험하는 **'글로벌 가족/지인 박물관 네트워크'**이다.
우리는 데이터를 중앙 서버에 가두지 않고, 각 개인의 저장소(BYOS)에 보관된 독립적인 박물관들을 '웜홀(Wormhole)'을 통해 매끄럽게 연결하여 무한한 관계의 우주를 탐험하는 경험을 제공한다.

2. Tree Layout Algorithm (가족 트리 배치 알고리즘)
가족 트리의 배치는 시각적 안정감과 직관적인 관계 파악을 최우선으로 고려하며, 다음의 논리적 원칙을 따른다.

초점의 원칙 (세대 제한):

트리는 현재 화면의 주인공인 '관장 부부(Main Person)'를 중심으로 렌더링된다.

인지적 과부하를 막기 위해 한 화면에 표시되는 윗세대는 부모 세대까지만 렌더링하는 것을 원칙으로 한다.

균형의 원칙 (Center Alignment):

부모 세대는 항상 자녀들 전체 너비의 중앙에 위치하여 시각적 안정감을 유지한다.

부모의 X 좌표 = (첫째 자녀의 중심 X + 막내 자녀의 중심 X) / 2 로직을 적용한다.

혈통 연결의 직관성:

연결선은 부부 공동의 중앙이 아니라, 해당 혈통을 가진 개인의 중심축에서 시작하여 부모로 연결된다.

규격의 준수:

모든 노드 배치 간격, 그리드 유닛, 부부 박스 크기 등 수치화된 규격은 **ORGCELL_CODING_RULES.md의 [블록 규격]**을 절대적으로 따른다.

3. Presentation & UX (애니메이션 및 렌더링 UX)
새로운 박물관(도메인)에 입장하거나 중심 인물이 변경될 때, 사용자에게 '이야기가 펼쳐지는 듯한' 프리미엄 경험을 제공한다.

순차적 등장 (Staggered Reveal):

모든 노드가 한 번에 나타나지 않고, 관장 부부부터 시작하여 ORGCELL_CODING_RULES.md에서 정한 표준 딜레이 수치에 따라 순차적으로 페이드인(Fade-in) 된다.

카메라 자동 맞춤 (Auto-Fit & 줌아웃):

노드가 추가될 때마다 전체 가족이 화면 밖으로 잘리지 않도록 뷰포트가 자동으로 줌아웃(Scale-down) 되며 모든 인원을 한 화면(Bounding Box) 안에 담아낸다.

4. Fallback & Data Handling (예외 처리 및 데이터 철학)
데이터가 비어있거나 외부 스토리지 연결이 지연될 때도 서비스는 절대 멈추지 않아야 한다.

우아한 실패 (Graceful Degradation):

사진 응답이 지연되더라도 트리의 뼈대(이름, 관계 선)는 즉시 그려져야 하며, 사진은 백그라운드에서 로딩된다.

가상 노드 (Virtual Node) 생성:

데이터가 비어있는 부모 자리에 '가상 노드'를 생성하여 시각적 틀을 유지하고, **"아직 개관전입니다"**라는 메시지로 데이터 입력을 유도한다.

5. Core Navigation: Wormhole Architecture
다른 박물관으로 이동하거나 중심 인물 변경 시, 이전 트리 상태(캐시, 뷰포트 등)는 **완벽하게 초기화(Hard Reset)**되어야 한다.

이전 화면의 잔재(Ghost Node)가 새 트리에 겹치는 것을 절대 허용하지 않는다.